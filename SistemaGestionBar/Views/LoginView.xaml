<UserControl x:Class="SistemaGestionBar.Views.LoginView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:b="clr-namespace:SistemaGestionBar.Behaviors"
             xmlns:vm="clr-namespace:SistemaGestionBar.ViewModels"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=vm:LoginViewModel}"
             d:DesignHeight="820"
             d:DesignWidth="1360">

    <UserControl.InputBindings>
        <KeyBinding Key="Enter" Command="{Binding IngresarCommand}" />
    </UserControl.InputBindings>

    <UserControl.Resources>
        <!-- Paleta y estilos sencillos para un login moderno, limpio y minimalista -->
        <SolidColorBrush x:Key="PrimaryBrush" Color="#111827" />
        <SolidColorBrush x:Key="AccentBrush" Color="#2D89EF" />
        <SolidColorBrush x:Key="SubtleBrush" Color="#6B7280" />

        <Style x:Key="InputBase" TargetType="Control">
            <Setter Property="Height" Value="44" />
            <Setter Property="Margin" Value="0,6,0,6" />
            <Setter Property="Padding" Value="12,8" />
        </Style>

        <Style x:Key="SimpleTextBox" TargetType="TextBox" BasedOn="{StaticResource InputBase}">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="SimplePasswordBox" TargetType="PasswordBox" BasedOn="{StaticResource InputBase}">
            <Setter Property="Background" Value="White" />
            <Setter Property="BorderBrush" Value="#E5E7EB" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}" />
            <Setter Property="FontSize" Value="13" />
        </Style>

        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Height" Value="46" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Padding" Value="14,6" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Margin" Value="0,12,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#F3F4F6">
        <Border Width="440" Padding="28" Background="White" CornerRadius="12" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="4" Opacity="0.12" />
            </Border.Effect>

            <StackPanel>
                <!-- Header -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,0,12">
                    <Border Width="48" Height="48" Background="{StaticResource AccentBrush}" CornerRadius="10">
                        <TextBlock Text="B" Foreground="White" FontSize="20" FontWeight="Bold" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </Border>
                    <StackPanel Margin="12,0,0,0">
                        <TextBlock Text="Sistema Gestión Bar" FontWeight="Bold" Foreground="{StaticResource PrimaryBrush}" />
                        <TextBlock Text="Accede a tu cuenta" FontSize="12" Foreground="{StaticResource SubtleBrush}" />
                    </StackPanel>
                </StackPanel>

                <TextBlock Text="Iniciar sesión" FontSize="20" FontWeight="SemiBold" Foreground="{StaticResource PrimaryBrush}" Margin="0,6,0,8" />
                <TextBlock Text="Introduce tus credenciales para continuar" FontSize="12" Foreground="{StaticResource SubtleBrush}" Margin="0,0,0,12" />

                <!-- Mensaje de estado (ViewModel) -->
                <TextBlock Text="{Binding Mensaje}" TextWrapping="Wrap" Margin="0,0,0,8" FontSize="12">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Setter Property="Foreground" Value="#10B981" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding HayMensaje}" Value="True">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding MensajeEsError}" Value="True">
                                    <Setter Property="Foreground" Value="#EF4444" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>

                <!-- Inputs enlazados al LoginViewModel -->
                <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource SimpleTextBox}" ToolTip="Usuario o correo electrónico" />
                <PasswordBox Style="{StaticResource SimplePasswordBox}" PasswordChar="●" b:PasswordBoxAssist.Enlazar="True" b:PasswordBoxAssist.Password="{Binding Clave, Mode=TwoWay}" />

                <Button Style="{StaticResource PrimaryButton}" Content="Entrar" Command="{Binding IngresarCommand}" />

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,14,0,0">
                    <TextBlock Text="¿No tienes una cuenta?" Foreground="{StaticResource SubtleBrush}" FontSize="12" />
                    <TextBlock Text=" Registrarse" Foreground="{StaticResource AccentBrush}" FontSize="12" FontWeight="SemiBold" Cursor="Hand" />
                </StackPanel>

                <!-- Informational box (kept for usability during design) -->
                <Border Background="#F4F7FB" CornerRadius="10" Padding="12" Margin="0,18,0,0">
                    <StackPanel>
                        <TextBlock Text="Usuarios de prueba" FontSize="11" FontWeight="Bold" Foreground="#7A8699" Margin="0,0,0,4"/>
                        <TextBlock Text="admin@bar.com · vendedor@bar.com · mesero@bar.com" FontSize="11" Foreground="#7A8699"/>
                        <TextBlock Text="Contraseña: 12345678" FontSize="11" Foreground="#7A8699"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
