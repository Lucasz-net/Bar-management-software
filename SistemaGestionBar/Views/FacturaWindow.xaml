<Window x:Class="SistemaGestionBar.Views.FacturaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Factura" Height="600" Width="800" WindowStartupLocation="CenterOwner" ResizeMode="CanResize">

    <Grid Background="#F7F9FB" Margin="12">
        <Border Background="White" CornerRadius="8" Padding="18" BorderBrush="#E6E9EE" BorderThickness="1">
            <DockPanel LastChildFill="True">
                <StackPanel DockPanel.Dock="Top">
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Stretch">
                        <StackPanel Orientation="Vertical">
                            <TextBlock Text="Factura" FontSize="20" FontWeight="Bold" />
                            <TextBlock Text="Sistema Gestión Bar" Foreground="#374151" />
                        </StackPanel>
                        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Margin="0,0,0,0">
                            <TextBlock Text="{Binding Factura.Numero}" FontWeight="Bold" HorizontalAlignment="Right" />
                            <TextBlock Text="{Binding Factura.FechaEmision, StringFormat={}{0:dd/MM/yyyy HH:mm}}" HorizontalAlignment="Right" />
                        </StackPanel>
                    </StackPanel>

                    <Separator Margin="0,10" />

                    <Grid Margin="0,6">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Cliente:" FontWeight="SemiBold" />
                            <TextBlock Text="{Binding Factura.ClienteNombre}" />
                        </StackPanel>
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="Importe:" FontWeight="SemiBold" HorizontalAlignment="Right" />
                            <TextBlock Text="{Binding Factura.Importe, StringFormat={}{0:C0}}" HorizontalAlignment="Right" />
                        </StackPanel>
                    </Grid>

                    <Border Background="#FAFAFC" CornerRadius="6" Padding="8" Margin="0,12,0,0">
                        <DataGrid ItemsSource="{Binding Factura.Lineas}" AutoGenerateColumns="False" HeadersVisibility="Column" CanUserAddRows="False" CanUserResizeRows="False" IsReadOnly="True">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Producto" Binding="{Binding NombreProducto}" Width="*" />
                                <DataGridTextColumn Header="Cant." Binding="{Binding Cantidad}" Width="70" />
                                <DataGridTextColumn Header="P. unit." Binding="{Binding PrecioUnitario, StringFormat={}{0:C0}}" Width="90" />
                                <DataGridTextColumn Header="Subtotal" Binding="{Binding Subtotal, StringFormat={}{0:C0}}" Width="110" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>

                    <Grid Margin="0,12,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="200" />
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="1">
                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                <TextBlock Text="Total:" FontWeight="Bold" Margin="0,0,8,0" />
                                <TextBlock Text="{Binding Factura.Importe, StringFormat={}{0:C0}}" FontWeight="Bold" />
                            </StackPanel>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="Imprimir" Width="100" Margin="0,0,8,0" Click="Imprimir_Click" />
                    <Button Content="Cerrar" Width="100" Click="Cerrar_Click" />
                </StackPanel>
            </DockPanel>
        </Border>
    </Grid>
</Window>
